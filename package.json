{
  "name": "trygc-dashboard-published-build",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Locally runnable reconstruction of the published TryGC dashboard build extracted from Vercel.",
  "scripts": {
    "start": "node server.mjs",
    "preview": "node server.mjs",
    "build:spa": "npm run build --prefix site"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
